# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# LaTeX build products
*.aux
*.log
*.out
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz

# Raw training logs and per-process scratch (regenerated by run_all.sh).
# The published result tables under results/ ARE tracked: they are the per-seed
# records behind every table in the paper and in the response document.
logs/
nohup.out
results/runtime/          # machine-dependent timings, re-measured per machine

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
